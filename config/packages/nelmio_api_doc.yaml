nelmio_api_doc:
    documentation:
        servers:
            - url: '%env(resolve:API_BASE_URL)%'
              description: API base
        info:
            title: 'Backend Product – API'
            description: 'Catalogue, commandes, auth JWT'
            version: '1.0.0'
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc(\.json)?$)
